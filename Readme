
//Open three terminals, 2 for the client and one for the server

make -> compiles
make runServer -> runs Server
make runClient x2 -> runs Clients
make clean -> cleans bin folder
make runDoc -> generates javadoc
make doc_clean -> cleans the doc folder

